{
  "purpose": "One trip case and one pass case per registered control. Every probe here was executed against its hook and its result observed before being written; the merge drops any probe lacking an observed field.",
  "merged_from": "13 authoring agents",
  "probes": [
    {
      "hook": "session-start-orientation.py",
      "event": "SessionStart",
      "path": ".claude/hooks/session-start-orientation.py",
      "trip": {
        "stdin": {
          "hook_event_name": "SessionStart",
          "source": "startup",
          "session_id": "probe0"
        },
        "expect": "warn",
        "why": "This hook has no conditional gate: every SessionStart must produce a non-empty orientation block. The real failure mode it can hide is its fail-open branch, which still exits 0 and still prints valid JSON but with additionalContext set to the empty string. So the condition tested is 'emits a populated orientation', not 'refuses something'.",
        "observed": "exit 0, 3032 bytes of JSON on stdout, zero on stderr. additionalContext began '[VAULT ORIENTATION 2026-08-23 20:54] / Active project: Agent-Governance-Research / Status: active / Last action: HARNESS AUDIT COMPLETE...' and carried 10 open task lines, so the project discovery, the STATE.md read and the task_plan.md read all ran. A hook_fire record landed in the redirected HOOK_ACTIVITY_LOG_PATH: decision=orient, detail=project=Agent-Governance-Research chars=2876. Side effect worth knowing: it shells out to .claude/scripts/cost-summary.py with a 4s timeout, so this probe is slower than the others and depends on that script existing."
      },
      "pass": {
        "stdin": "not json at all",
        "must_not": [
          "deny",
          "block"
        ],
        "why": "Malformed stdin is the negative case that matters for a SessionStart hook: its contract is that it can never break session start. It must swallow the parse failure, exit 0, and never return a deny or a block. It is NOT expected to go silent, because emitting orientation is unconditional by design.",
        "observed": "exit 0, still printed the full orientation JSON, nothing on stderr, no permissionDecision field anywhere in the output. NOTE this stdin is a raw string, not an object: the hook reads stdin as text and only the shared session_from() helper parses it, so a JSON object is not required to exercise this path."
      }
    },
    {
      "hook": "checkpoint.py",
      "event": "PostToolUse",
      "path": ".claude/hooks/checkpoint.py",
      "trip": {
        "stdin": {
          "tool_name": "Bash",
          "tool_input": {
            "command": "ls"
          },
          "session_id": "probe-b1",
          "cwd": "{{vault}}"
        },
        "expect": "warn",
        "why": "Trigger is time-file based, NOT payload based: the hook reads %USERPROFILE%/.claude/last-checkpoint and warns when now-last >= 60s, with the escalated [CHECKPOINT] text at >= 300s. PRECONDITION the runner must set: USERPROFILE pointed at a scratch dir whose .claude/last-checkpoint holds an epoch at least 300s in the past. No stdin field can force this trip; the payload only supplies session_id for the activity log.",
        "observed": "Ran with USERPROFILE=<tmp>/home and last-checkpoint = now-400. stdout was the PostToolUse additionalContext JSON beginning '[CHECKPOINT] 5min since last save. Write current status to STATE.md NOW.' followed by the SAVE CHECK routing text; exit code 0. One hook-activity record went to the redirected HOOK_ACTIVITY_LOG_PATH: decision=checkpoint, detail=idle_s=400, session=probe-b1."
      },
      "pass": {
        "stdin": {
          "tool_name": "Bash",
          "tool_input": {
            "command": "ls"
          },
          "session_id": "probe-b1",
          "cwd": "{{vault}}"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "Identical payload run immediately after the trip. The trip run rewrote last-checkpoint to now, so diff < 60 and the 60s throttle suppresses the reminder. Proves the hook is throttled rather than nagging on every tool call.",
        "observed": "Second consecutive run, same stdin: stdout was exactly '{}' with no additionalContext and no reminder text, exit code 0. No new hook-activity record, since the logger sits past the throttle's early return."
      }
    },
    {
      "hook": "qmd-rerank-default-guard.py",
      "event": "PreToolUse",
      "path": ".claude/hooks/qmd-rerank-default-guard.py",
      "trip": {
        "stdin": {
          "tool_name": "mcp__qmd__query",
          "tool_input": {
            "collection": "agr-kb",
            "query": "hook governance"
          },
          "session_id": "probe-b1"
        },
        "expect": "deny",
        "why": "rerank_is_missing_or_not_false returns True when the key is absent, which is the common real-world shape: the MCP tool schema default is rerank:true, and that value hangs indefinitely on this GPU-less machine. Exact tool-name match on mcp__qmd__query.",
        "observed": "stdout JSON carried hookSpecificOutput.permissionDecision 'deny' with marker '(rerank absent)' inside permissionDecisionReason; exit code 0. One governance record went to the redirected GOVERNANCE_LOG_PATH: event=deny, hook=qmd-rerank-default-guard, marker='rerank absent'. I also ran the second trip shape, tool_input rerank:true explicit: also denied, marker '(rerank not false)', so an explicit true earns no exemption."
      },
      "pass": {
        "stdin": {
          "tool_name": "mcp__qmd__query",
          "tool_input": {
            "collection": "agr-kb",
            "query": "hook governance",
            "rerank": false
          },
          "session_id": "probe-b1"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "The one compliant call shape: rerank present and exactly boolean false, the doctrine-mandated form in CLAUDE.md's Memory Recall section. It must pass silently, or the guard would make the very call it demands impossible.",
        "observed": "No stdout, no stderr, exit code 0, and no governance record appended."
      }
    },
    {
      "hook": "claude-md-provenance-check.py",
      "event": "PostToolUse",
      "path": ".claude/hooks/claude-md-provenance-check.py",
      "trip": {
        "stdin": {
          "tool_name": "Edit",
          "tool_input": {
            "file_path": "{{vault}}/CLAUDE.md",
            "old_string": "x",
            "new_string": "## CRITICAL RULE: Never Skip The Gate\n\nYou must always run the gate before shipping.\n"
          },
          "session_id": "PROBE-B2-SYNTH"
        },
        "expect": "warn",
        "why": "file_path is the exact vault-root CLAUDE.md (is_vault_root_claude_md). new_string matches two rule-shaped patterns (a heading line and the literal CRITICAL RULE) and matches none of the four provenance patterns (no [[wikilink]], no feedback_/finding_/decision_/reference_ memo name, no Projects/*/work/ path, no dated parenthetical). rule_shaped AND NOT has_provenance is the sole WARN branch. The hook is warn-only by design (docstring: SEVERITY: WARN only, stderr + exit 0, NEVER blocks), so warn is the maximum true trip, not a downgraded expectation.",
        "observed": "exit 0. One line on stderr: '[claude-md-provenance-check WARN] behavioral-rule change without a one-line origin citation; add the originating decision or research file inline ... (advisory - edit was kept.)'. stdout empty. HOOK_ACTIVITY_LOG_PATH was honored: the warn record went to the isolated temp sink, not the live hook-activity.jsonl."
      },
      "pass": {
        "stdin": {
          "tool_name": "Edit",
          "tool_input": {
            "file_path": "{{vault}}/CLAUDE.md",
            "old_string": "87 agents",
            "new_string": "88 agents"
          },
          "session_id": "PROBE-B2-SYNTH"
        },
        "must_not": [
          "deny",
          "block",
          "warn"
        ],
        "why": "Same file, so this exercises content discrimination and not path scoping: a count-maintenance edit carries no heading, no CRITICAL RULE marker, and no bolded modal-verb rule line, so rule_shaped() is False and the hook takes the silent allow branch. This is the exact maintenance-edit case the docstring promises stays silent.",
        "observed": "exit 0, stdout empty, stderr empty. Silent."
      }
    },
    {
      "hook": "reviewer-scope-violation-check.py",
      "event": "PreToolUse",
      "path": ".claude/hooks/reviewer-scope-violation-check.py",
      "trip": {
        "stdin": {
          "agent_type": "architect-reviewer",
          "tool_name": "Edit",
          "tool_input": {
            "file_path": "{{vault}}/CLAUDE.md"
          },
          "transcript_path": "",
          "session_id": "PROBE-B2-SYNTH"
        },
        "expect": "deny",
        "why": "This is the hook's canonical block case and its own TP1 fixture scenario: agent_type is in REVIEWER_AGENTS, a file_path is present, the path does NOT match REVIEW_OUTPUT_RE (Rule A), and the file DOES exist on disk (so Rule C does not apply), which is exactly Step 4, print hookSpecificOutput.permissionDecision deny.",
        "observed": "DEFECT, NO DENY. Invoked exactly as registered in settings.local.json (python.exe <hook>, no extra env): exit 0, stdin never parsed. stdout was the hook's own smoke-test report: 'Smoke tests: reviewer-scope-violation-check.py', four [PASS] lines, then 'Results: 4 passed, 0 failed out of 4 tests'. Cause: the __main__ block calls main() only when os.environ['HOOK_SMOKE_TEST'] == '1'; with that variable unset it runs the self-test harness instead. Re-running the identical stdin with HOOK_SMOKE_TEST=1 produced the correct {\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"permissionDecision\": \"deny\", ...}} at exit 0, which proves main() is sound and the production entrypoint is the break. See defects_found."
      },
      "pass": {
        "stdin": {
          "tool_name": "Edit",
          "tool_input": {
            "file_path": "{{vault}}/CLAUDE.md"
          },
          "transcript_path": "",
          "session_id": "PROBE-B2-SYNTH"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "A main-session edit: agent_type is absent and transcript_path is empty, so the fallback lookup finds nothing, agent_type stays outside REVIEWER_AGENTS and the fast-path allow fires. Benign: the guard is about reviewer sub-agents editing what they review, not about ordinary edits.",
        "observed": "exit 0, no deny. WARNING, this pass leg is vacuous in production: stdout was again the smoke-test report, not an evaluated allow, because the same __main__ guard means the payload is never read. With HOOK_SMOKE_TEST=1 the same stdin returned a genuine silent allow (empty stdout, exit 0). Until the entrypoint is fixed, this control's pass leg proves nothing about discrimination."
      },
      "defect_fixed": "HA-A-080/HA-A-081 fixed 2026-08-23; the probe caught the fix via strict xfail"
    },
    {
      "hook": "routing-table-validation.py",
      "event": "PreToolUse",
      "path": ".claude/hooks/routing-table-validation.py",
      "trip": {
        "stdin": {
          "tool_name": "Edit",
          "tool_input": {
            "file_path": "{{vault}}/CLAUDE.md",
            "new_string": "MUST DISPATCH: nonexistent-fake-agent"
          }
        },
        "expect": "deny",
        "why": "All four gates pass. (a) target is CLAUDE.md. (b) 'MUST DISPATCH:' is a definitive dispatch position. (c) the token matches AGENT_NAME_RE. (d) it resolves to nothing in registry.json agents or skills and is in neither NON_AGENT_IDENTIFIERS nor DEPRECATED_ALLOWLIST. That is the Delta-5 Tier A condition: a doctrine edit introducing an undispatchable routing target.",
        "observed": "exit 0. stdout: {\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"permissionDecision\": \"deny\", \"permissionDecisionReason\": \"ROUTING_BROKEN: The following agent name(s) appear in a dispatch position in '{{vault}}/CLAUDE.md' but resolve to nothing in registry.json: \\\"nonexistent-fake-agent\\\". ...\"}}. Sink: decision 'deny', detail 'CLAUDE.md nonexistent-fake-agent'. Classification: deny."
      },
      "pass": {
        "stdin": {
          "tool_name": "Edit",
          "tool_input": {
            "file_path": "{{vault}}/CLAUDE.md",
            "new_string": "MUST DISPATCH: pm-orchestrator, process-qa"
          }
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "Same file, same dispatch position, same comma-separated shape; only the names differ. Both resolve in registry.json (pm-orchestrator under agents, process-qa under skills), so gate (d) fails and the edit must be allowed. This is the false-positive guard: the hook must not deny every MUST DISPATCH line it sees.",
        "observed": "exit 0. Nothing on stdout or stderr. Sink: decision 'allow', detail 'CLAUDE.md'. Classification: silent."
      }
    },
    {
      "hook": "bias-guard.py",
      "event": "SubagentStart",
      "path": ".claude/hooks/bias-guard.py",
      "trip": {
        "stdin": {
          "agent_type": "adversarial-reviewer",
          "session_id": "test-probe-b4"
        },
        "expect": "warn",
        "why": "adversarial-reviewer is in the EVALUATORS list (lines 35-43), so the hook injects the BLIND ANALYSIS RULE notice as SubagentStart additionalContext. This hook is an injector, not a blocker: its only enforcement surface is that stdout payload, so 'the notice is present' is the whole control.",
        "observed": "exit 0, stdout {\"hookSpecificOutput\":{\"hookEventName\":\"SubagentStart\",\"additionalContext\":\"BLIND ANALYSIS RULE: You are an evaluator agent. Evaluate ONLY what you are given against the stated criteria...\"}}; hook-activity record decision=inject detail=adversarial-reviewer."
      },
      "pass": {
        "stdin": {
          "agent_type": "general-purpose",
          "session_id": "test-probe-b4"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "general-purpose is not an evaluator, so the else branch prints the empty object and injects nothing. Confirms the guard is selective rather than firing on every subagent.",
        "observed": "exit 0, stdout exactly '{}' (empty JSON object, no additionalContext); hook-activity record decision=skip detail=general-purpose. Grader note: '{}' is stdout output, so a naive 'printed anything equals warn' rule would misread this benign case. Assert on absence of additionalContext, not on empty stdout."
      }
    },
    {
      "hook": "subagent-governance.py",
      "event": "SubagentStart",
      "path": ".claude/hooks/subagent-governance.py",
      "trip": {
        "stdin": {
          "hook_event_name": "SubagentStart",
          "agent_type": "general-purpose",
          "agent_id": "probe-batch5",
          "session_id": "probe-batch5"
        },
        "expect": "warn",
        "why": "This hook is an unconditional context injector, not a gate: for ANY parseable non-empty JSON payload it appends an agent_type/agent_id line to subagent-governance.log and prints the AGENT GOVERNANCE additionalContext block. The only live condition is 'stdin parsed to JSON', so a well-formed SubagentStart payload is the genuine trigger. The probe proves the control is alive and actually emits its payload rather than being registered-but-inert.",
        "observed": "rc=0, 530 bytes stdout: {\"hookSpecificOutput\": {\"hookEventName\": \"SubagentStart\", \"additionalContext\": \"AGENT GOVERNANCE: Use multiple perspectives when analyzing - do not settle on the first answer. Cite specific evidence ... Blind analysis rule: ...\"}}. Nothing on stderr. Classified warn."
      },
      "pass": {
        "stdin": {
          "hook_event_name": "SubagentStart",
          "agent_type": "general-purpose",
          "agent_id": "probe-batch5-benign",
          "session_id": "probe-batch5"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "HONEST LIMITATION: this hook has no discriminating trigger, so no benign SubagentStart payload can make it go silent. The pass case therefore asserts only the property that IS falsifiable here, that it never denies and never blocks a subagent start. Verified against the code: the sole outputs are the additionalContext print and exit code 0; there is no permissionDecision path and no non-zero exit anywhere in the file.",
        "observed": "rc=0, 530 bytes stdout carrying the same additionalContext block, 0 bytes stderr. No permissionDecision field, no exit 2. Behaves identically to the trip case, as the code predicts."
      }
    },
    {
      "hook": "bash-safety-guard.py",
      "event": "PreToolUse",
      "path": ".claude/hooks/bash-safety-guard.py",
      "trip": {
        "stdin": {
          "tool_name": "Bash",
          "tool_input": {
            "command": "git push --force origin main"
          },
          "session_id": "probe-b7"
        },
        "expect": "deny",
        "why": "Matches the BLOCKED_PATTERNS row 'git force-push' (git, optional global options, push, .*--force). Force-push is the one git operation the 2026-08-05 Wiktor ruling explicitly kept as a hard deny when normal push was demoted to a warning, so this row is the Gate-1 reversibility floor itself. The command survives strip_inert_contexts (no quoted body, no -m message, no heredoc), so the deny loop sees it verbatim.",
        "observed": "stdout: hookSpecificOutput.permissionDecision = \"deny\", reason \"BASH SAFETY: Blocked 'git force-push'. Command: git push --force origin main...\", exit code 0. A deny record went to the redirected GOVERNANCE_LOG_PATH with pattern=git force-push. No gate1_surface_degraded alarm appeared in the redirected GATE1_ALARM_LOG_PATH, so _irreversible_surface imported cleanly and the deny came from the canonical surface, not the frozen fallback snapshot."
      },
      "pass": {
        "stdin": {
          "tool_name": "Bash",
          "tool_input": {
            "command": "git status --short"
          },
          "session_id": "probe-b7"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "A read-only git invocation. It is deliberately a git command rather than an unrelated one, so it proves the guard discriminates within the git surface instead of denying anything containing 'git'. It matches no BLOCKED_PATTERNS row, no WARN_PATTERNS row, no curl-write predicate, and no Windows reserved redirect target.",
        "observed": "No stdout, no stderr, exit code 0 (the allow-silently path). Nothing was appended to the redirected governance log."
      }
    },
    {
      "hook": "subagent-quality-check.py",
      "event": "SubagentStop",
      "path": ".claude/hooks/subagent-quality-check.py",
      "trip": {
        "stdin": {
          "hook_event_name": "SubagentStop",
          "agent_type": "general-purpose",
          "agent_id": "probe-b7",
          "last_assistant_message": "ok",
          "session_id": "probe-b7"
        },
        "expect": "block",
        "why": "classify_subagent_output CHECK 1: len(message) < 5 returns check_1_empty. The tool-call escape hatch does not apply because it only engages when last_assistant_message is blank after strip(), and 'ok' is not; there is also no agent_transcript_path in the payload, and _final_turn_used_tool fails closed on a missing path. NOTE ON MECHANISM, do not read this as a mismatch: a SubagentStop hook blocks by printing {\"decision\": \"block\"} on stdout and exiting 0, NOT by exit code 2. If a classifier keys on exit code alone it will label this 'warn'; the semantic class is block, and the regression it guards against is the hook falling silent.",
        "observed": "stdout: {\"decision\": \"block\", \"reason\": \"Agent output is empty (2 chars). Produce a substantive response.\"}, exit code 0. A block record went to the redirected GOVERNANCE_LOG_PATH with check_failed=check_1_empty. I also ran CHECK 2 with last_assistant_message = \"I cannot do that.\": {\"decision\": \"block\", ...matched: 'i cannot'...}, exit 0."
      },
      "pass": {
        "stdin": {
          "hook_event_name": "SubagentStop",
          "agent_type": "general-purpose",
          "agent_id": "probe-b7",
          "last_assistant_message": "- Checked 3 files\n- No defects found",
          "session_id": "probe-b7"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "36 chars, so CHECK 1 (len < 5) and CHECK 3 (len > 500) do not apply, and CHECK 2 does not fire because the message carries no refusal keyword. It is also the shape the 2026-06-02 overfire fix was written for: a short bulleted NEGATIVE finding. A hook that blocked this would be over-firing on valid work.",
        "observed": "No stdout, exit code 0. A pass record went to the redirected GOVERNANCE_LOG_PATH with message_len=36."
      }
    },
    {
      "hook": "subagent-scope-check.py",
      "event": "SubagentStop",
      "path": ".claude/hooks/subagent-scope-check.py",
      "trip": {
        "stdin": {
          "hook_event_name": "SubagentStop",
          "agent_id": "probe-batch8-nobaseline",
          "agent_type": "probe-agent"
        },
        "expect": "warn",
        "why": "A baseline EXISTS for this agent_id and the working tree gained a file under it, so current-minus-baseline is a genuine one-file delta and non-empty new_changes is the warn condition. REWRITTEN 2026-08-24: the previous version tripped by supplying NO baseline, which made the hook report the entire dirty tree as new. That was the hook's bug, not its trip condition. Measured before the fix: 5,249 of 9,641 live records had no baseline, averaged 407 'new' files against 0.8 for records that had one, and held 161.2 MB of the 162.8 MB log. A probe pinned to a bug turns green into red the moment the bug is fixed, which is what happened here.",
        "observed": "Originally observed against the live vault while it held 4 untracked entries: exit 0, stderr '[SCOPE-CHECK] ... modified 4 new file(s)', activity decision=warn. That made the probe decay: a later commit left the tree clean, current minus baseline became empty, and the same payload went silent, which the suite read as the control failing to fire. Re-based 2026-08-23 on a scratch repository holding exactly one untracked file, so the trip is now a function of the probe's own inputs. Side effects on the live vault: none, the sink and the state file follow SUBAGENT_SCOPE_ROOT into scratch."
      },
      "pass": {
        "stdin": {
          "hook_event_name": "SubagentStop",
          "agent_id": "probe-batch8-paired",
          "agent_type": "probe-agent"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "The benign case is a subagent that changed nothing. It REQUIRES the paired seed: the SubagentStart in seed_stdin stores the scratch tree as that agent_id's baseline, so the SubagentStop above computes new_changes = current minus baseline = empty and takes the allow branch. Without the seed this same payload warns, which is what makes the pair a real discriminator rather than a formality. must_not holds unconditionally since the hook has no deny or block path anywhere in its code.",
        "observed": "Ran the pair back to back. SubagentStart: exit 0, silent, activity decision=pass detail='baseline probe-agent'. SubagentStop: exit 0, nothing on stdout or stderr, activity decision=allow. The pop removed the seeded id, so the live state file was left as found.",
        "seed_stdin": {
          "hook_event_name": "SubagentStart",
          "agent_id": "probe-batch8-paired",
          "agent_type": "probe-agent"
        }
      },
      "scratch_repo": "dirty-with-baseline:probe-batch8-nobaseline"
    },
    {
      "hook": "user-prompt-submit.py",
      "event": "UserPromptSubmit",
      "path": ".claude/hooks/user-prompt-submit.py",
      "trip": {
        "stdin": {
          "prompt": "audit the hook chain and report drift",
          "session_id": "probe10"
        },
        "expect": "warn",
        "why": "A substantive prompt that is not in the skip_list and carries no agent_id/agent_type, so is_subagent is False and classifier_reminder survives to the output. The injected additionalContext must contain 'MANDATORY: Invoke the task-classifier skill'. CAVEAT for whoever reads this gate: this hook is an injector with no deny or block tier, and it prints a non-empty additionalContext on EVERY input (the context bar is built even with no transcript_path), so it can never be classified 'silent'. A checker that only looks at exit code plus stdout-presence will grade the pass case below as 'warn' too. The real assertion is on the SUBSTRING 'MANDATORY: Invoke the task-classifier skill' being present here and absent in the pass case.",
        "observed": "exit 0, stdout = {\"hookSpecificOutput\": {\"hookEventName\": \"UserPromptSubmit\", \"additionalContext\": \"DISPLAY RULE: ... `CTX (bar) 0% | 0K/200K | ?` | MANDATORY: Invoke the task-classifier skill before responding to EVERY message. No exceptions. ... Missing fields = incomplete classification.\"}}"
      },
      "pass": {
        "stdin": {
          "prompt": "ok",
          "session_id": "probe10"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "'ok' is a literal member of the skip_list at line 112, so the conversational-follow-up branch clears classifier_reminder and only the context bar is injected. Benign: no enforcement text is added. Also confirmed the other suppression path works, a payload carrying agent_type suppresses the reminder for subagents.",
        "observed": "exit 0, stdout = {\"hookSpecificOutput\": {\"hookEventName\": \"UserPromptSubmit\", \"additionalContext\": \"DISPLAY RULE: Begin every response with this exact line in a code block on its own, no changes: `CTX (bar) 0% | 0K/200K | ?`\"}} with NO 'MANDATORY' substring. Same run with {\"prompt\":\"audit the hook chain\",\"agent_type\":\"general-purpose\"} also emitted no MANDATORY text."
      }
    },
    {
      "hook": "skill-step-reminder.py",
      "event": "PostToolUse",
      "path": ".claude/hooks/skill-step-reminder.py",
      "trip": {
        "stdin": {
          "tool_name": "Skill",
          "tool_input": {
            "skill": "process-qa"
          },
          "session_id": "probe11",
          "cwd": "{{vault}}"
        },
        "expect": "warn",
        "why": "tool_input.skill lowercases to 'process-qa', a key in PROCESS_REMINDERS (line 49), so the hook passes the process-skill filter at line 93 and prints a hookSpecificOutput additionalContext reminder.",
        "observed": "exit 0, stdout = {\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"PROCESS REMINDER: You MUST follow these steps in order. Step 1: List every verifiable claim (QA SCOPE block). ... Step 5: Escalate failures, do NOT fix them, report them.\"}}; one record appended to the redirected hook-activity sink: hook=skill-step-reminder decision=remind detail=process-qa session=probe11. Classified warn."
      },
      "pass": {
        "stdin": {
          "tool_name": "Skill",
          "tool_input": {
            "skill": "pm"
          },
          "session_id": "probe11",
          "cwd": "{{vault}}"
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "Same event shape and a real installed skill, but 'pm' is not a process-* skill, so PROCESS_REMINDERS.get returns None and line 94 returns before any output. This isolates the process-skill filter rather than the event shape.",
        "observed": "exit 0, no stdout, no stderr, no record appended to the activity sink. Classified silent."
      }
    },
    {
      "hook": "reply-style-guard.py",
      "event": "Stop",
      "path": ".claude/hooks/reply-style-guard.py",
      "trip": {
        "stdin": {
          "session_id": "probe-dark3",
          "transcript_path": "{{home}}/AppData/Local/Temp/probe-dark3/reply_trip.jsonl"
        },
        "transcript_fixture": {
          "path": "{{home}}/AppData/Local/Temp/probe-dark3/reply_trip.jsonl",
          "lines": [
            {
              "type": "assistant",
              "message": {
                "content": [
                  {
                    "type": "text",
                    "text": "This **one** has **far** too **much** **bold** **emphasis** in it."
                  }
                ]
              }
            }
          ]
        },
        "expect": "block",
        "why": "The fixture's last assistant message carries 5 bold spans against the limit of 4, so find_violations returns a bold violation and main() must exit 2 with the REPLY STYLE stderr block.",
        "observed": "Authoring run 2026-08-31: exit 2, stderr beginning 'REPLY STYLE: rewrite this response before sending it. / Too much bold: 5 bold spans (limit is 4).' Nothing on stdout."
      },
      "pass": {
        "stdin": {
          "session_id": "probe-dark3",
          "transcript_path": "{{home}}/AppData/Local/Temp/probe-dark3/reply_pass.jsonl"
        },
        "transcript_fixture": {
          "path": "{{home}}/AppData/Local/Temp/probe-dark3/reply_pass.jsonl",
          "lines": [
            {
              "type": "assistant",
              "message": {
                "content": [
                  {
                    "type": "text",
                    "text": "A perfectly plain reply."
                  }
                ]
              }
            }
          ]
        },
        "must_not": [
          "deny",
          "block"
        ],
        "why": "A plain last assistant message must pass. Authoring run: exit 0, zero bytes on both streams."
      }
    }
  ],
  "waivers": [
    {
      "hook": "token-breakdown.py",
      "event": "Stop",
      "reason": "Telemetry-only Stop hook with NO deny/block/warn surface to assert: it never writes stdout or stderr and never blocks, so every outcome it can produce classifies as 'silent' and the trip/pass expectation vocabulary cannot express its firing. Waived from the taxonomy, NOT left untested. I built a two-line fake transcript (one user entry, one assistant entry with model claude-opus-4-7, usage input=100 output=50 cache_read=2000 cache_creation=10, and one Bash tool_use block) and ran the hook with stdin {\"hook_event_name\":\"Stop\",\"session_id\":\"probe-b1\",\"transcript_path\":\"<tmp>/probe-b1-transcript.jsonl\"}. It emitted exactly one token_breakdown record to the redirected GOVERNANCE_LOG_PATH: turn_total_tokens=2160, main_session matching the fixture, tool_calls={\"Bash\":1}, turn_cost_usd=0.008438, price_rates_as_of=2026-05-23. Both suppression paths verified in the same run: stop_hook_active:true emitted nothing, and an all-zero-usage transcript emitted nothing (record count stayed at 1). The hook fires correctly; asserting it needs a probe runner that can check for an emitted governance record rather than a stdout decision. FIRST ATTEMPT FAILED for a probe-authoring reason worth recording: I passed the transcript path in MSYS form (/c/Users/...), which Windows Python cannot resolve, so os.path.exists returned False and main() returned early and silently. Any transcript-reading probe in this suite MUST use the <drive>:/ path form or it will look green while proving nothing."
    },
    {
      "hook": "dark-zone-check.py",
      "event": "Stop",
      "reason": "Monitoring-only by construction: the file contains no print() and no write to stdout or stderr anywhere (grep over the whole file returns zero matches), and its module docstring says 'Does NOT block'. Its only output channel is emit_event to the governance sink, so no input can produce a deny, block or warn observable and the rubric has nothing to assert. This waives the OBSERVABLE, not the behaviour: both cases were built and run. TRIP (fixtures/probe3-darkzone-trip.jsonl, one Agent tool_use plus a response with zero citation patterns and zero Write/Edit blocks) produced {\"event\": \"dark-zone\", \"agent_count\": 1, \"citation_count\": 0, \"files_written\": 0, \"ratio\": 0.0, \"severity\": \"high\"}. PASS (fixtures/probe3-darkzone-pass.jsonl, identical dispatch, response text 'Per code-reviewer: the agent found a missing null check at line 42') produced {\"citation_count\": 2, \"ratio\": 2.0, \"severity\": \"low\"}. Both exited 0 with empty stdout and stderr. The hook does discriminate; it discriminates into a log file rather than into the turn. If the suite later gains a sink-assertion mode, this control is probeable there with the two fixtures already in place."
    },
    {
      "hook": "mcp-circuit-breaker-record.py",
      "event": "PostToolUse",
      "reason": "No deny/block/warn path exists in the file: it is the PostToolUse recorder half of the circuit breaker and contains no print to stdout or stderr and no non-zero exit (its own docstring: 'Exit codes: 0 always'). The enforcement lives in the PreToolUse half, mcp-circuit-breaker.py, which is a different control. Its real observable is a state mutation, which the trip/pass vocabulary cannot express, so a probe expecting deny|block|warn would red-flag a healthy hook. VERIFIED OUT OF BAND rather than assumed: fed {tool_name: mcp__probeserver__thing, tool_response: {is_error: true, error: 'MCP error -32000: connection closed'}} and got exit 0, stdout and stderr empty, _state/mcp-circuit-breaker.json gaining probeserver.failures=['2026-08-23T18:52:54Z'], and a hook-activity row decision='failure' detail='probeserver n=1'. Benign twin (tool_response.content=[{text: 'ok, 3 results'}]) gave exit 0, silence, failures reset to [] with last_success_at set, decision='recovery'. So it does fire and it does discriminate. SECOND REASON IT IS NOT HERMETICALLY PROBEABLE: STATE_FILE is hardcoded to _state/mcp-circuit-breaker.json with no env override, so any run mutates live breaker state; this probe used a nonexistent server key and then restored the file from a byte-identical snapshot."
    },
    {
      "hook": "git-credential-scope-check.py",
      "event": "SessionStart",
      "reason": "The trigger is not in the event. The hook reads stdin only to recover a session id for telemetry; its verdict comes entirely from machine state, namely `git config --global --get credential.usehttppath` plus a 24h cache at _state/git-credential-scope.json. No stdin payload can make it warn, and none can make it stay silent either, so both halves of a stdin-driven probe would be decoration. Neither the git config path nor the cache file has an env override the fragment schema could carry. VERIFIED OUT OF BAND that the warn path is real: with the cache file removed and GIT_CONFIG_GLOBAL pointed at an empty config, exit 0 with SessionStart additionalContext '[GIT CREDENTIAL SCOPE: WARNING] credential.usehttppath is NOT set to true...' and hook-activity decision='warn' detail='key-absent'; with usehttppath=false, the same 461-byte warning and detail='value=false'; with usehttppath=true, silent and decision='ok'. Live machine state at probe time was healthy (usehttppath=true), so on this machine today no input trips it. See defects_found for the cache-suppression result. The live cache file was snapshotted and restored byte-identically."
    },
    {
      "hook": "governance-log.py",
      "event": "Stop",
      "reason": "Logging-only by construction: the file contains no print, no sys.exit(2), and no hookSpecificOutput, and its own docstring says 'Does NOT block; logging only.' Its trip is an appended turn_summary record, which the deny|block|warn expectation vocabulary cannot express. VERIFIED OUT OF BAND with a hand-written fake transcript: a Stop payload whose transcript holds an assistant text block with 'TASK TYPE: Build / DOMAIN: hooks / MUST DISPATCH: process-qa, pm' plus an Agent tool_use produced exit 0, empty stdout and stderr, and exactly one line in GOVERNANCE_LOG_PATH: event='turn_summary', type='Build', domain='hooks', must_dispatch='process-qa, pm', agents=['pm-orchestrator'], agent_count=1. The benign twin (a transcript with no classification line) produced exit 0, silence, and zero additional lines, via the `if not last_type: return` guard. So it records and it discriminates. Isolation was clean here: GOVERNANCE_LOG_PATH and HOOK_ACTIVITY_LOG_PATH both redirect, so no live sink was written."
    },
    {
      "hook": "subagent-scope-check.py",
      "event": "SubagentStart",
      "reason": "This registration is the baseline-capture half only. The SubagentStart branch writes a `git status --porcelain` baseline into _state/subagent-scope-baselines.json, logs decision='pass', and returns 0 with no output. The single warn in this file (the '[SCOPE-CHECK] sub-agent ... modified N new file(s)' line on stderr) is unreachable from SubagentStart: it lives in the SubagentStop branch, which is a separate registration and a separate control (batch 8). VERIFIED OUT OF BAND: {hook_event_name: 'SubagentStart', agent_id: 'probe-agent-b6', agent_type: 'general-purpose'} gave exit 0, stdout and stderr empty, a baseline recorded with 4 porcelain entries, and hook-activity decision='pass' detail='baseline general-purpose'. Also probed the degenerate case: a payload with no hook_event_name falls through to the unknown-event no-op (exit 0, silent, decision='skip'), so this hook is entirely dependent on the platform supplying hook_event_name. STATE_FILE and LOG_FILE are hardcoded absolute paths with no env override, so the probe mutated live state; the baselines file was snapshotted and restored byte-identically."
    },
    {
      "hook": "lint-cadence-trigger.py",
      "event": "SessionStart",
      "reason": "Not probeable through stdin, and its output envelope cannot discriminate a trip from a pass. Every fire decision comes from the age of five state files whose paths are computed from the hook's own location (.claude/hooks/_state/{lint,governance-mine,setup-audit,work-triage,telemetry-check}-cadence.json) with NO env override, so no stdin field can move it; the payload is used only to stamp a session id on the activity record. Worse for probe purposes, main() ALWAYS prints the SessionStart envelope and exits 0 whether or not any suggestion fired, so both a trip and a pass classify as 'warn' and a pass assertion of must_not deny/block would be satisfied by any input at all: a control that fires on everything. There is also no reachable benign input on this machine today, since two builders are permanently overdue. NOT a defect: I verified the cadence logic really fires by importing the module and calling each builder directly, which returned [SETUP-AUDIT CADENCE] (76 days) and [WORK-TRIAGE CADENCE: OVERDUE] (44 days) with the other four correctly empty, and by running the hook end to end on {\"hook_event_name\":\"SessionStart\",\"source\":\"startup\"}, whose stdout carried both strings plus the KNOWN-DISPATCH-NAMES REGEN line at exit 0. A real gate for this hook needs a state-file precondition the runner writes, which means writing live vault state, so it is out of scope here."
    },
    {
      "hook": "memory-nudge.py",
      "event": "Stop",
      "reason": "The Stop registration has no deny, block or warn path in its code at all, by design. _handle_stop increments turns_since_memory_write in the state file and returns; the module docstring states 'Prints nothing, ever' for Stop, and the nudge text is only ever emitted by the SessionStart registration (a different control, batch 9). So the only classification a Stop probe can ever produce is 'silent', and an expect of deny/block/warn would be a knowingly false expectation. Its actual enforcement IS observable as a state side effect, and I verified it rather than assuming: with MEMORY_NUDGE_STATE_PATH redirected to a scratch file, two successive {\"hook_event_name\":\"Stop\"} payloads moved turns_since_memory_write 0 -> 1 -> 2 (exit 0, no stdout both times), and a third payload carrying stop_hook_active: true correctly left it at 2, so the Stop-chain double-count guard works. If this suite later grows a state-delta assertion type, this control belongs in it."
    },
    {
      "hook": "mcp-qmd-health-probe.py",
      "event": "SessionStart",
      "reason": "Not payload-constructible. The hook reads stdin ONLY to derive session_id for logging; every branch verdict comes from environment state, namely .mcp.json resolvability and the exit code and latency of a real 'node qmd.js status' subprocess. No SessionStart payload can select a branch, and MCP_JSON_PATH and STATE_FILE are both derived from __file__ with no env override. I did not stop there: I copied the hook byte-for-byte into a sandbox fake vault (Temp/probe8/fakevault/.claude/hooks/) so HOOKS_DIR, MCP_JSON_PATH and STATE_FILE all resolved inside the sandbox, and exercised both failure branches for real. (A) No .mcp.json present produced stdout additionalContext '[QMD HEALTH PROBE: R8] qmd probe cannot resolve CLI path from .mcp.json ... Recall-layer health UNKNOWN', exit 0. (B) A .mcp.json pointing node at <drive>:/nonexistent/qmd.js produced '[QMD HEALTH PROBE] qmd recall layer UNREACHABLE at session start (probe exit 1: ... MODULE_NOT_FOUND)', exit 0, and the sandbox state file received the expected failures entry plus last_probe_failure. Both warn branches are live code, not dead code. A trip against the REAL vault was deliberately rejected as a suite probe because record_failure() writes into the live _state/mcp-circuit-breaker.json failures list that mcp-circuit-breaker.py trips on at THRESHOLD=3 within 600s: a probe that seeds those is a probe that can disable the recall layer for real sessions. See defects_found, because the live hook is currently emitting that failure warning against a HEALTHY qmd anyway."
    },
    {
      "hook": "session-start-log.py",
      "event": "SessionStart",
      "reason": "No deny/block/warn surface exists to probe. The hook is a pure telemetry writer: its entire effect is one appended session_start record, and the suite's _classify reads only stdout, stderr and the exit code, so its real output is invisible to the gate. VERIFIED BY EXECUTION, not assumed: with GOVERNANCE_LOG_PATH redirected to a scratch file, the payload {\"session_id\":\"probe12-session\",\"source\":\"startup\",\"transcript_path\":...} exited 0 with zero bytes on stdout and stderr and wrote exactly one line to the scratch log, {\"event\": \"session_start\", \"hook\": \"session-start-log\", \"session\": \"probe12-session\", \"source\": \"startup\"}. So the control is live, not dark; it is unassertable by THIS harness. Its one stdout/stderr-visible branch is the 'DASHBOARD (date): alerts' stderr line, which fires only when the PREVIOUS CALENDAR DAY's real aggregate trips a threshold (classifier_blocks>=1, qa_fails>=1, agent_warn_downgrades>=3, quick_ratio>0.95). That branch is driven by DASHBOARD_TARGET_DATE and by the live governance log, neither of which the harness sets, so a probe built on it would return a different verdict on different days. Asserting it needs a sink-reading assertion (scratch governance log contains one session_start), which the current probe schema cannot express."
    },
    {
      "hook": "session-start.sh",
      "event": "SessionStart",
      "reason": "This suite invokes every probe with the Python interpreter, so a bash or PowerShell hook cannot be executed by the current runner. The hook is NOT exempt from coverage on merit: it became visible only when the enumerator was widened past .py, which is itself an audit finding. Closing this needs an interpreter-aware invoke path, tracked as a follow-up."
    },
    {
      "hook": "restore-compact.sh",
      "event": "SessionStart",
      "reason": "This suite invokes every probe with the Python interpreter, so a bash or PowerShell hook cannot be executed by the current runner. The hook is NOT exempt from coverage on merit: it became visible only when the enumerator was widened past .py, which is itself an audit finding. Closing this needs an interpreter-aware invoke path, tracked as a follow-up."
    },
    {
      "hook": "ralph-stop-hook.ps1",
      "event": "Stop",
      "reason": "This suite invokes every probe with the Python interpreter, so a bash or PowerShell hook cannot be executed by the current runner. The hook is NOT exempt from coverage on merit: it became visible only when the enumerator was widened past .py, which is itself an audit finding. Closing this needs an interpreter-aware invoke path, tracked as a follow-up."
    },
    {
      "hook": "hook-write-regression-gate.py",
      "event": "PostToolUse",
      "reason": "WAIVED 2026-08-24 TO STOP A RUNNING FORK BOMB THAT THIS PROBE CAUSED. The probe's trip stdin was a Write to .claude/hooks/bias-guard.py, a hook .py file, with no HOOK_REGRESSION_GATE_TARGET_DIR, and the probe schema had no env mechanism at all. So the gate did what it is built to do: run the FULL live hook suite. That suite contains test_controls_fire.py, which runs this probe, which spawns another full suite. Unbounded recursion, each level about 200s of pytest. Measured while the session was idle: 79 concurrent hook-write-regression-gate processes with 81 pytest children, ALL under 2 minutes old with a median age of 1.1 minutes, so roughly 40 new full-suite runs per minute rather than a leak from earlier work. It also appended to a live aggregate 21 times per 30 idle seconds via the wiki-citation probe each recursion level reached, and it is the most likely source of the %TEMP% churn that made the regression gate's own green-suite test flake. Process termination is blocked by the permission classifier, so removing the probe is the available way to stop it: every in-flight suite that reaches this point after this edit reads the new manifest and does not re-spawn. The author of defect D7 flagged exactly this shape and I recorded it as a caveat rather than a recursion, which was the wrong call. RESTORE CONDITION: the probe returns once the runner supports a per-probe env map (defect D12's second half) so the trip can set HOOK_REGRESSION_GATE_TARGET_DIR at an isolated red mini-suite, and once the gate itself carries a re-entrancy guard so no future caller can reproduce this. The removed probe definition is preserved verbatim below.",
      "removed_probe": {
        "hook": "hook-write-regression-gate.py",
        "event": "PostToolUse",
        "path": ".claude/hooks/hook-write-regression-gate.py",
        "trip": {
          "stdin": {
            "tool_name": "Write",
            "tool_input": {
              "file_path": ".claude/hooks/bias-guard.py"
            },
            "session_id": "test-probe-b4"
          },
          "expect": "warn",
          "why": "is_gated_hook_write() is true for a .py path under .claude/hooks/ that is not in _state/archive/aggregates/__pycache__, so the gate leaves the fast path and runs pytest; a non-zero pytest exit takes the SUITE RED branch (line 216) and emits the PostToolUse additionalContext warning. REQUIRES the hook's own documented isolation env (module docstring lines 25-28): HOOK_REGRESSION_GATE_TARGET_DIR={{vault}}/.claude/hooks/_probe_fragments/fixtures/.b4-redsuite (a one-test deliberately-failing fixture committed for this probe, in a dot-directory so `pytest .claude/hooks/` never recurses into it, verified 0 collection hits) and HOOK_REGRESSION_GATE_LOG_PATH=<temp>. WITHOUT that env the gate runs the live 900+ test suite, which is green, and the same stdin is externally silent: a harness that omits the env will score this control as dead when it is not.",
          "observed": "exit 0, stdout {\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"[HOOK-WRITE REGRESSION GATE - SUITE RED] The edit to .claude/hooks/bias-guard.py landed with a FAILING regression suite (pytest exit 1). DO NOT report success. Root-cause and fix before proceeding. Suite tail: ... test_probe_red_marker ...\"}}; hook-activity record decision=warn detail='suite red (exit 1) after edit of bias-guard.py'."
        },
        "pass": {
          "stdin": {
            "tool_name": "Write",
            "tool_input": {
              "file_path": "Inbox/probe-note.md"
            },
            "session_id": "test-probe-b4"
          },
          "must_not": [
            "deny",
            "block"
          ],
          "why": "A non-.py path outside .claude/hooks/ fails is_gated_hook_write(), so main() returns at the fast path (line 189) with no pytest run at all. Same tool and same event shape as the trip; only the gated-ness of the path differs.",
          "observed": "exit 0, stdout empty, stderr empty (silent, and instant, no pytest child spawned)."
        },
        "timeout": 400,
        "timeout_why": "this control runs the entire pytest suite inside its own check, so it is legitimately slower than every other hook; a timeout here would be the harness failing, not the control"
      }
    },
    {
      "hook": "staleness_check.py",
      "event": "SessionStart",
      "reason": "PROBED LIVE AND CONFIRMED FIRING, but not trippable through THIS harness's channel: the probe schema delivers stdin only, and the hook ignores stdin entirely (its inputs are the manifest file and argv). It DOES expose a first-class --manifest argv override, and a direct out-of-band invocation was executed as evidence on 2026-08-31: a fixture manifest with a 2026-01-01 embedded stamp and max_age_days 7 produced exit 0 with additionalContext '[STALENESS] ... probe-fixture-stale (STALE): age 242.4d exceeds max 7d -> echo rerun-me', and the same day's real session start carried its live all-PASS block. Behavior further pinned by test_staleness_check.py (26 tests incl. hook mode and fail-loud manifest errors), and since 2026-08-31 it writes hook-activity.jsonl via the shared log_fire helper. Revisit if the probe schema ever grows an argv channel; the earlier wording, which denied that any override channel existed, was wrong and was corrected after the 2026-08-31 architect review."
    }
  ],
  "defects_reported_by_authors": [
    {
      "hook": "qmd-recall-nudge.py",
      "detail": "Scope gap, not a dead hook. It reads only tool_input['path'], so a Grep issued with no path argument never fires, even though that is the case where the Grep actually sweeps the whole vault including Resources/KB and is therefore the strongest qmd-recall trigger. Verified by running it: {tool_name: Grep, tool_input: {pattern: pairedItem}, cwd: {{vault}}} exits 0 with zero output, while the same Grep with path=Resources/KB fires. The author flagged the mechanism in a comment (path is 'optional; defaults to cwd') but not the consequence, and the hook never falls back to the payload's cwd. Left unfixed per the no-unsolicited-changes rule."
    },
    {
      "hook": "wiki-citation-check.py",
      "detail": "Doctrine says this gate blocks; the code says it never does. CLAUDE.md 'Wiki Layer Invariants' item 2 states: 'Hook-level (wiki-citation-check.py PostToolUse Write): on every Write to a #wiki-tagged file: verifies source: field present ... Mismatch -> blocks write with SOURCE_DRIFT.' The hook's own docstring contradicts that: '2 = hard block (currently DISABLED, too risky for v1; activate after empirical baseline)'. Confirmed empirically: a fixture citing a source path that does not exist on disk produced an ORPHAN_CITATION finding of severity error, printed as advisory additionalContext, exit 0, write kept. So layer 2 of the three fabrication-mitigation layers named in CLAUDE.md is advisory at runtime while doctrine records it as blocking. The detector fires correctly; the divergence is enforcement strength, constitution versus code, which is the same shape as the audit finding this suite exists for. Resolving it is Wiktor's decision (raise the hook to blocking, or correct CLAUDE.md), not a test change."
    },
    {
      "hook": "work-verification-check.py",
      "detail": "Not a non-firing defect, a harness hazard worth recording before the merge: this hook signals both HARD blocks (CHECK 1 and CHECK 1b) by printing {\"decision\": \"block\"} to stdout and returning exit code 0. A merged runner that classifies 'block' as 'exit code 2', which is the rubric this batch was handed, will score a correctly-firing hard gate as a warn, or fail it outright if it asserts on the exit code. The same applies to every Stop-event gate in the vault. The merge step needs a Stop-protocol-aware classifier."
    },
    {
      "hook": "hook-write-regression-gate.py",
      "detail": "NOT a failure-to-trip: it tripped correctly under its documented isolation env. Probeability caveat the harness must carry. This control's trip is invisible unless HOOK_REGRESSION_GATE_TARGET_DIR points at a red suite. Run with only the two standard log-redirect env vars, the identical trip stdin makes the gate execute the live 900+ test hooks suite; while that suite is green the hook exits 0 with empty stdout, so the control reads as silent and dead. A suite that replays stdin alone will therefore record a false negative on exactly the hook whose job is to catch regressions."
    },
    {
      "hook": "git-credential-scope-check.py",
      "detail": "A genuine trigger produced no warning, observed directly. Sequence, all three runs using the same SessionStart stdin: (1) ran against the healthy live machine, which wrote a fresh result='ok' into _state/git-credential-scope.json; (2) immediately re-ran with GIT_CONFIG_GLOBAL pointed at a config with credential.usehttppath absent, the exact condition the hook exists to catch, and got exit 0 with EMPTY stdout and hook-activity decision='cached-ok'; (3) deleted the cache file, re-ran the identical trigger, and got the full warning. So _cache_is_fresh_ok() short-circuits the live check for 24h after any healthy session start, and during that window the NDA-relevant misconfiguration (host-level work-account credential becoming the GCM fallback for every github.com push) is not reported at all. This is documented in the hook's own docstring as an intentional cache and the direction is deliberate (only a fresh 'ok' may skip work; a 'warn' or a missing file always re-runs), so it is design rather than a coding bug. It is recorded here because it is exactly the failure shape this suite hunts: registered, healthy, green, and for up to 24 hours at a stretch unable to fire on a real trigger. Worth a human ruling on whether a 24h blind window is the right price for one `git config` subprocess call per session, when the hook's whole purpose is to catch a config line going missing between sessions."
    },
    {
      "hook": "mcp-qmd-health-probe.py",
      "detail": "INVERTED-SENSE DEFECT (it fires, but wrongly). PROBE_TIMEOUT_SECONDS=20 was calibrated against a BARE 'qmd status' run, while run_probe() invokes it with capture_output=True. Measured on this machine 2026-08-23, same command and same env: stdout to DEVNULL = 5.95s rc=0; stdout through a pipe, which is the hook's own call shape = 23.58s rc=0. A cold run was 37.1s rc=0. So a HEALTHY qmd exceeds the hook's own budget. I ran the real hook once with a benign SessionStart payload and after exactly 20.27s it returned '[QMD HEALTH PROBE] qmd recall layer UNREACHABLE at session start (probe timed out after 20s)', a false positive. This is not hypothetical: the live _state/mcp-circuit-breaker.json already carried last_probe_failure 'probe timed out after 20s' at 2026-08-23T14:14:08Z before I touched anything, plus last_query_probe_failure 'query-probe timed out after 19s' at 2026-08-22T17:41:10Z. Every false positive appends a real timestamp to qmd.failures, and mcp-circuit-breaker.py trips at THRESHOLD=3 inside WINDOW=600s, so three session starts within ten minutes would make the health probe itself deny every mcp__qmd__* call, disabling the recall layer it exists to protect. The same pipe-versus-bare calibration error applies to QUERY_PROBE_TIMEOUT_SECONDS=19, calibrated at a 6.24s warm ceiling. DISCLOSURE: my one real-hook run appended 2026-08-23T18:56:18Z to that live failures list; it now holds 2 entries, both older than the 600s window, so nothing is currently tripped."
    },
    {
      "hook": "mcp-circuit-breaker.py",
      "detail": "The breaker cannot deny in this environment. MCP_HEALTH_FAIL_OPEN=1 is set as a Windows USER-scope environment variable ([Environment]::GetEnvironmentVariable('MCP_HEALTH_FAIL_OPEN','User') returns 1; Machine scope is empty), so every hook subprocess Claude Code spawns inherits it and _is_override_set() returns True on every evaluation. Proven by running the registered hook against a state file seeded with 3 failures seconds old: with the inherited environment it printed nothing and exited 0; with identical state and stdin under 'env -u MCP_HEALTH_FAIL_OPEN' it printed permissionDecision deny. The documented purpose of that variable is a per-call escape hatch (bypass the breaker for the next call), not a standing setting. This is the audit's own pattern: registered, green sibling tests (test_mcp_circuit_breaker.py sets and clears the variable inside the test process, so the suite never sees the ambient one), and zero enforcement. The fix is an environment change, not a code change, so it is reported and not applied."
    },
    {
      "hook": "process-step-check.py",
      "detail": "Not an enforcement defect, a classifier hazard for the merged suite: this hook blocks with stdout {\"decision\":\"block\"} at exit code 0. A runner that defines 'block' as exit code 2 (the taxonomy in the authoring brief) scores every trip of this hook as a mere 'warn', and would score a gutted version that printed any stray line as equally green. The suite must read the decision field for Stop-event hooks."
    },
    {
      "hook": "suite-wide (probe schema)",
      "detail": "Three of the five controls in this batch (mcp-circuit-breaker.py, memory-nudge.py, and the fire path of user-prompt-state-inject.py) gate on an on-disk state file rather than on the event payload, so a stdin-only probe cannot construct their trip and would score a working hook as silent. Two are waived here for exactly that reason. If the probe schema gained a per-probe 'env' map and a 'state_seed' (path plus JSON body written before the run and restored after), all three become genuinely probeable; memory-nudge.py already exposes MEMORY_NUDGE_STATE_PATH for this, mcp-circuit-breaker.py exposes no state-path override at all."
    },
    {
      "hook": "session-start-log.py",
      "detail": "Second isolation bypass. The best-effort dashboard block imports _daily_aggregate and calls write_aggregate(yesterday). _daily_aggregate carries its own hardcoded LOG_PATH and AGG_DIR and ignores GOVERNANCE_LOG_PATH, so it reads the LIVE governance log and WRITES the live artifact .claude/hooks/aggregates/daily/<date>.json. Observed: a single probe invocation rewrote aggregates/daily/2026-08-22.json (mtime moved to Aug 23 20:53). Running this hook under the control suite mutates a production aggregate, and the aggregate it computes is of live data even when the sink is redirected. Not fixed here."
    },
    {
      "hook": "memory-dedup-check.py",
      "detail": "Not a trigger defect, an OBSERVABILITY defect in the gate. Every exit path prints, the benign one printing a literal {}. Under the suite's _classify (non-empty stdout plus exit 0 equals warn) the duplicate-detected verdict and the all-clear verdict carry the SAME label, warn. So the trip assertion for this hook would still pass if the hook were gutted to print {} unconditionally, which is exactly the never-fires-but-looks-healthy class this suite exists to catch. The stdout bodies do differ and were read by hand on both runs; the gate cannot see the difference. Closing it needs either a silent benign path in the hook or a stdout-substring assertion in the probe schema."
    }
  ],
  "dropped_at_merge": []
}
